# 待办.md

## 当前待办

- 首页首屏已按“大标题 + 赛道轮廓主视觉 + 底部事实栏”重排，待用户评审是否继续微调视觉密度、赛道轮廓透明度或首屏文案。
- 按 project-workbench/next-steps.md 继续推进：真实授权图片替换与来源整理、重点车型独立文件拆分、剩余事实核验、移动端专项复查、非 Porsche/Mercedes 品牌文案风格统一。

## 低优先级遗留项（未修复，待决定）

- useParallax 在车型 hero 可见期间持续 requestAnimationFrame，可改为仅 scroll/resize 触发。
- SPA 未匹配路由渲染 404 页面但 HTTP 状态为 200，如在意 SEO 需平台侧单独处理。
- `npm run dev` 使用 macOS 专用 `caffeinate`，非 macOS 环境无法直接使用。
- Porsche 919 Hybrid Evo 文案写 5:19.55，圈速表写 5:19.546，属于展示精度差异，待决定统一格式。

## 前端优化第一期（2026-08-30 启动）

- ~~将 `project-workbench/tasks-phase-1.md` 交给 Kimi Code 执行（T1 构建提速 / T2 SEO 元信息 / T3 README 修正 / T4 动效降级），完成后回 Kimi 客户端审核。~~ 2026-08-30 已由 Kimi Code 完成，4 个 commit（`phase1-T1`~`phase1-T4`），构建 1.3–2s。待 Jinke 审核后部署 Netlify，部署后需验证 `https://nordschleife-guide.netlify.app/og-image.jpg` 返回 200 并跑 Lighthouse SEO。
- ~~PRD 待 Jinke 确认的问题：og 图方案（A 现有图 vs B 定制卡片）、样式拆分粒度。~~
- PRD 两个待确认问题已由 Jinke 拍板（og 图方案 B、样式混合拆分），已写入 tasks-phase-1.md / tasks-phase-2.md。
- tasks-phase-1.md 中"`src/data/corners.js` 和 `src/data/brands.js` 已不存在"与实际不符：两文件仍在，是各 `*-records/` 目录的聚合层；README 已按实际结构修正。

## 前端优化第三期（2026-08-30 完成）

- ~~T7 移动端回归 + T8 全站审计~~ 已由 Kimi Code 完成并提交 `phase3-T7` / `phase3-T8` 两个 commit。T7 六项检查全过（修复抽屉遮罩失效回归、Tab 焦点循环、焦点归还、8 处触控目标 44px）；T8 全站 CLS 清零、a11y 全站 100、弯角列表 LCP 10.2s→4.8s。报告见 `project-workbench/t7-mobile-regression.md` / `t8-audit-report.md`。**待 Jinke 审核，通过后部署 Netlify 即收官。**
- T8 遗留项（待 Jinke 决定排期）：弯角照片体积优化（image-delivery 最高可省 ~1.5MB，属内容资产处理）；弯角列表 LCP 仍 4.8s（主因图片体积）；unused JS ~30KiB 与 render-blocking ~150ms（收益小）；llms-txt（Lighthouse 新建议项）。
